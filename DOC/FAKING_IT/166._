       

        I am number one big liar. Now saving for video 166.
        Code body of : F_TILEBOI_HI9_TE7_LA3_TLX_TLY_DPC
    

        var   dpx =(   0 ); //: @dpx@ : DataPixel - X    ://     // [###]
        var   dpy =(   0 ); //: @dpy@ : DataPixel - Y    ://     // [###]
                                                                 // [###]
        //- h3x =(           ( i_hi9 % 3 ) ); -//                // [###]
        //- h3y =Math.floor( ( i_hi9 / 3 ) ); -//                // [###]
                                                                 // [###]
        var mmx = d_hi9_mmx[ hi9 ]; //:MasterMemoryCell.X://     // [###]
        var mmy = d_hi9_mmy[ hi9 ]; //:MasterMemoryCell.Y://     // [###]
                                                                 // [###]
        var dia     = d_te7_dia[ i_te7 ];                        // [###]
        var lat     = d_te7_lat[ i_te7 ];                        // [###]
        var lmx     = d_la3_lmx[ i_la3 ];                        // [###]
        var lmy     = d_la3_lmy[ i_la3 ];                        // [###]
        var lahotel =( 5 * lat );                                // [###]
        ASS( i_tlx >= 0 && i_tlx <=(lahotel-1) , "[LAT:TLX]" );  // [###]
        ASS( i_tly >= 0 && i_tly <=(lahotel-1) , "[LAT:TLY]" );  // [###]
                                                                 // [###]
        var r5x = i_tlx - Math.floor( i_tlx / lat );             // [###]
        var r5y = i_tly - Math.floor( i_tly / lat );             // [###]
        var g25 = ( r5x + ( r5y * 5 ) ); //:( r5i===g25 )://     // [###]
        var t_x = i_tlx - ( r5x * lat );                         // [###]
        var t_y = i_tly - ( r5y * lat );                         // [###]
                                                                 // [###]
        //:------------------------------------------://         // [###]
        //: [r4x,r4y] is the [DAT]a   position.      ://         // [###]
        //: [r5x,r5y] is the [REN]der position.      ://         // [###]
        //:---------------------------:              ://         // [###]
                                    //: [DAT]!=[REN] ://         // [###]
        var r4x = d_g25_r4x[ g25 ]; //:  r4x != r5x  ://         // [###]
        var r4y = d_g25_r4y[ g25 ]; //:  r4y != r5y  ://         // [###]
        var rdi = d_g25_rdi[ g25 ]; //:--------------://         // [###]
        ASS( r4x + ( r4y * 4 ) === rdi , "[R4X_R4Y_4_RDI]" );    // [###]
                                                                 // [###]
        dpx += (mmx*512)+(r4x*128)+dia+(lat*lmx) ;               // [###]
        dpy += (mmy*512)+(r4y*128)+dia+(lat*lmy) ;               // [###]
